<header class="sticky-top">
    <nav class="navbar navbar-expand-lg navbar-dark py-5">
        <div class="container">
            <a class="navbar-brand logo" href="{{ path('home_index') }}">Lexique</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon "></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav w-100">
                    <li class="nav-item">
                        <a class="nav-link active " aria-current="page" href="{{ path('word_index') }}">À propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('contact_index') }}">Contact</a>
                    </li>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item profil">
                        <a class="nav-link active d-flex align-items-center" aria-current="page" href="{{ path('word_index') }}">
                            <span class="img-profil me-2"><i class="fas fa-user fa-xs"></i></span>
                            <p class="m-0"> {{ app.user.username }} </p>
                        </a>
                    </li>
                    <li class="nav-item logout">
                        <a class="nav-link active d-flex align-items-center" aria-current="page" href="/logout">
                        se déconnecter
                        </a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="{{ path('user_index') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <span class="img-profil">
                                <i class="fas fa-user fa-xs"></i>
                            </span>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item">Connecté en tant que</a></li>
                            <li><strong class="ps-3"> {{ app.user.username }} </strong></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('register') }}">Profil</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('lexicon_index') }}">Lexicons</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class='dropdown-item' href="/logout">logout</a></li>
                        </ul>
                    </li>
                    {% else %}
                        <li class="nav-item register">
                            <a class="nav-link active" aria-current="page" href="{{ path('register') }}">S'inscrire</a>
                        </li>
                        <li class="nav-item login">
                            <a class="nav-link active" aria-current="page" href="{{ path('login') }}">Se connecter</a>
                        </li>
                {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>
