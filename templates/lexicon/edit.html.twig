{% extends 'base.html.twig' %}

{% block title %}{{ 'lexicon.edit.title'|trans }}{% endblock %}

{% block body %}
    <div class="flex-fill bg-light py-5">
        <div class="container">
            <i class="fas fa-book-open fa-4x d-block text-center mb-4 dark"></i>
            <h2 class="title-page">{{ 'lexicon.edit.formtitle'|trans }}</h2>
            <div class="form-app border dark p-5">
                {{ include('lexicon/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                {{ include('lexicon/_delete_form.html.twig') }}
                <a href="{{ path('lexicon_index') }}">{{ 'lexicon.edit.return'|trans }}</a>
            </div>
        </div>
    </div>
{% endblock %}
