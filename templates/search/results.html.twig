{% extends 'base.html.twig' %}

{% block title %}Word index{% endblock %}

{% block body %}
    {# #Contenu #}
    <h1>Search results :</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Mots :</th>
        </tr>
        </thead>
        <tbody>
        {% for row in rows %}
            <tr>
                <td> <a href="{{ path('word_show', {'id': row.id}) }}"> {{ row.name }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">No results found.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lexicon_index') }}">Retour à la liste</a>
{% endblock %}
